export interface ThemeStatusColor {
  green: string;
  ligthBlue: string;
  teal: string;
  purple: string;
  orange: string;
  red: string;
  blue: string;
  gray: string;
  primaryLight: string;
  darkGreen: string;
  yellow: string;
  cyan: string;
}

export type ThemeStatusColorKey = keyof ThemeStatusColor;
